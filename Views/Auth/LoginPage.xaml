<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AstroBoy.Views.Auth.LoginPage"
             BackgroundColor="White"
             NavigationPage.HasNavigationBar="False">

    <Grid>
        <!-- Latar Belakang Biru Header -->
        <VerticalStackLayout HeightRequest="280" VerticalOptions="Start">
            <VerticalStackLayout.Background>
                <LinearGradientBrush EndPoint="1,0">
                    <GradientStop Color="#42c3ff" Offset="0.0" />
                    <GradientStop Color="#1e8cf0" Offset="1.0" />
                </LinearGradientBrush>
            </VerticalStackLayout.Background>

            <!-- Header Kece Astro Boy -->
            <VerticalStackLayout Spacing="0" HorizontalOptions="Center" Margin="0,70,0,0">
                <Label Text="ASTRO"
                       TextColor="White"
                       FontSize="42"
                       FontAttributes="Bold"
                       HorizontalTextAlignment="Center"
                       CharacterSpacing="2" />
                <Label Text="BOY APP"
                       TextColor="#8bd5ff"
                       FontSize="18"
                       FontAttributes="None"
                       HorizontalTextAlignment="Center"
                       CharacterSpacing="5"
                       Margin="0,-10,0,0" />
                <BoxView WidthRequest="40"
                         HeightRequest="3"
                         Color="White"
                         HorizontalOptions="Center"
                         Margin="0,10,0,0" />
            </VerticalStackLayout>
        </VerticalStackLayout>

        <!-- Area Form Putih -->
        <Border Margin="0, 180, 0, 0" BackgroundColor="White" StrokeThickness="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="30,30,0,0" />
            </Border.StrokeShape>

            <ScrollView>
                <VerticalStackLayout Padding="30" Spacing="15">

                    <Label Text="LOGIN"
                           FontSize="24"
                           TextColor="#1e8cf0"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           Margin="0,20,0,20" />

                    <!-- Input Email/Username -->
                    <VerticalStackLayout Spacing="0">
                        <Label Text="Username" FontSize="11" TextColor="#42c3ff" />
                        <Entry Placeholder="example@email.com"
                               Text="{Binding _Email}"
                               Keyboard="Email"
                               BackgroundColor="Transparent"
                               TextColor="Black" />
                        <BoxView HeightRequest="1" Color="#8bd5ff" Margin="0,5,0,0" />
                    </VerticalStackLayout>

                    <!-- Input Password (Tanpa Forget Password) -->
                    <VerticalStackLayout Spacing="0" Margin="0,10,0,0">
                        <Label Text="Password" FontSize="11" TextColor="#42c3ff" />
                        <Grid>
                            <Entry Placeholder="********"
                                   IsPassword="True"
                                   Text="{Binding Password}"
                                   BackgroundColor="Transparent"
                                   TextColor="Black" />
                            <Label Text="?" TextColor="#A1A1A1" HorizontalOptions="End" VerticalOptions="Center" Margin="0,0,5,0" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#8bd5ff" Margin="0,5,0,0" />
                    </VerticalStackLayout>

                    <!-- Pesan Error -->
                    <Label Text="{Binding ErrorMessage}" TextColor="Red" FontSize="12" HorizontalOptions="Center"/>

                    <!-- Tombol Login -->
                    <Button Text="LOGIN"
                            Command="{Binding LoginCommand}"
                            TextColor="White"
                            CornerRadius="25"
                            HeightRequest="50"
                            FontAttributes="Bold"
                            Margin="0,30,0,0"
                            WidthRequest="200"
                            HorizontalOptions="Center">
                        <Button.Background>
                            <LinearGradientBrush EndPoint="1,0">
                                <GradientStop Color="#42c3ff" Offset="0.0" />
                                <GradientStop Color="#1e8cf0" Offset="1.0" />
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>

                    <!-- Link ke Register -->
                    <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,20,0,0">
                        <Label Text="Don't have an account?" FontSize="10" TextColor="#1e8cf0"/>
                        <Label Text="Sign Up" TextColor="#1e8cf0" FontAttributes="Bold" FontSize="10">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding GoToRegisterCommand}" />
                            </Label.GestureRecognizers>
                        </Label>
                    </HorizontalStackLayout>

                </VerticalStackLayout>
            </ScrollView>
        </Border>
    </Grid>
</ContentPage>